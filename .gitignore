node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Note: .env file is intentionally public for Replit agent access
# All credentials are development/demo keys safe for public use
.config/
